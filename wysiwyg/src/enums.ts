// Vuetify3 size options
export enum ComponentSize {
  XS = 'x-small',
  SM = 'small',
  MD = 'medium',
  LG = 'large',
  XL = 'x-large',
  DF = 'default' // https://vuetifyjs.com/en/api/v-btn/#props-size
}

/**
 * Viewport size breakpoints.
 */
export enum ViewportSize {
  XS = 'xs',
  SM = 'sm',
  MD = 'md',
  LG = 'lg',
  XL = 'xl'
}